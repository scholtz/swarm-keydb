# Node.js Express example

Demonstrates `SwarmKeyDbService`, cache/session middleware, and streaming key scan.

## Run

```bash
cd examples/node-express
npm install
npm start
```

Ensure SwarmKeyDb server is running on `127.0.0.1:6379`.
