# React example app

Demonstrates `useSwarmValue`, `useSwarmPut`, `useSwarmDelete`, and `useSwarmKeys`.

## Run

```bash
cd examples/react-app
npm install
npm run dev
```

Ensure SwarmKeyDb server is running on `127.0.0.1:6379`.
